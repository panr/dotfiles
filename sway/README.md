```bash
sudo apt install sway swaylock swayidle waybar \
    wlr-randr brightnessctl \ # Display
    nmcli \ # Network
    bluez blueman \ # Bluetooth
    pulseaudio pactl pavucontrol # Audio
    gnome-keyring seahorse # Kerings
```
